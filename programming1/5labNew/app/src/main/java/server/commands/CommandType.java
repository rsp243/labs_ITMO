package server.commands;

/**
 * Enum Command Type. Support reading process of values. 
 */

public enum CommandType {
    INFO_WORKER,
    CITY_WORKER,
    COLLECTION_WORKER,
    FILE_WORKER,
    STATE_WORKER
}
